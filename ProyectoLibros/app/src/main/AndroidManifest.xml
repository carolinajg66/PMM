<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.carolina.proyectolibros">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".libros.LibrosActivity"
            android:label="@string/title_activity_libros"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".librosdetail.LibrosDetailActivity"
            android:label="@string/title_activity_libros_detail"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".addeditlibros.AddEditLibrosActivity"
            android:label="@string/title_activity_add_edit_libros"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>